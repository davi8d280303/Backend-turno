{
  "name": "backend-prestamos",
  "version": "1.0.0",
  "description": "Backend API para sistema de préstamos",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "node --test \"tests/*.test.js\"",
    "check": "node --check src/index.js && node --check src/routes/index.js && node --check src/config/supabaseClient.js && node --check src/services/supabaseHealthService.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
